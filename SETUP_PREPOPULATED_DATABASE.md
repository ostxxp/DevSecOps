# Подробная инструкция: Использование готовой базы данных Dependency Check

## Обзор

Вместо обновления базы данных в GitHub Actions (что вызывает проблемы с блокировкой), мы обновим базу данных локально, загрузим её в публичное хранилище, и будем скачивать в workflow.

---

## Шаг 1: Обновление базы данных локально

### 1.1. Установка Dependency Check локально (если ещё не установлен)

```bash
# Перейдите в домашнюю директорию
cd ~

# Скачайте Dependency Check
wget https://github.com/jeremylong/DependencyCheck/releases/download/v10.0.0/dependency-check-10.0.0-release.zip

# Или используйте curl
curl -L -O https://github.com/jeremylong/DependencyCheck/releases/download/v10.0.0/dependency-check-10.0.0-release.zip

# Распакуйте
unzip dependency-check-10.0.0-release.zip -d ~/dependency-check
chmod +x ~/dependency-check/dependency-check/bin/dependency-check.sh
```

### 1.2. Настройка Java (если нужно)

```bash
# Проверьте, установлена ли Java
java -version

# Если нет, установите через Homebrew
brew install openjdk@17

# Настройте JAVA_HOME
export JAVA_HOME=$(/usr/libexec/java_home -v 17)
export PATH="$JAVA_HOME/bin:$PATH"
```

### 1.3. Обновление базы данных

```bash
# Создайте директорию для базы данных
mkdir -p ~/.dependency-check/data

# Запустите обновление базы данных
# Это займёт 30-60 минут, но нужно сделать только один раз
~/dependency-check/dependency-check/bin/dependency-check.sh --updateonly

# Дождитесь завершения - вы увидите сообщение об успешном обновлении
```

**Важно:** Дождитесь полного завершения обновления. Это может занять 30-60 минут.

---

## Шаг 2: Проверка базы данных

```bash
# Проверьте, что база данных создана
ls -lh ~/.dependency-check/data/

# Должно быть много файлов (100+)
# Проверьте количество файлов
find ~/.dependency-check/data -type f | wc -l

# Должно быть больше 50 файлов
```

---

## Шаг 3: Архивирование базы данных

```bash
# Перейдите в директорию с базой данных
cd ~/.dependency-check

# Создайте архив базы данных
tar -czf dependency-check-db.tar.gz data/

# Проверьте размер архива (должен быть 100-500 MB)
ls -lh dependency-check-db.tar.gz

# Переместите архив в удобное место
mv dependency-check-db.tar.gz ~/Downloads/dependency-check-db.tar.gz
```

---

## Шаг 4: Загрузка базы данных в публичное хранилище

У вас есть несколько вариантов:

### Вариант A: GitHub Releases (рекомендуется)

1. **Создайте новый релиз в вашем репозитории:**
   - Перейдите на GitHub: `https://github.com/DevSecOps-F25/DevSecOps/releases`
   - Нажмите "Create a new release"
   - Tag: `v1.0.0-db` (или любое другое имя)
   - Title: `Dependency Check Database`
   - Описание: `Pre-populated Dependency Check database for CI/CD`

2. **Загрузите архив:**
   - В разделе "Attach binaries" перетащите файл `dependency-check-db.tar.gz`
   - Нажмите "Publish release"

3. **Получите URL для скачивания:**
   - После публикации URL будет: 
   ```
   https://github.com/DevSecOps-F25/DevSecOps/releases/download/v1.0.0-db/dependency-check-db.tar.gz
   ```

### Вариант B: GitHub Actions Artifacts (альтернатива)

Если Releases недоступны, можно использовать GitHub Actions Artifacts, но это сложнее.

### Вариант C: Другой публичный источник

- Google Drive (нужно сделать публичным)
- Dropbox (публичная ссылка)
- Любой другой файловый хостинг

---

## Шаг 5: Настройка workflow для скачивания базы данных

### 5.1. Откройте файл workflow

```bash
# Откройте файл в редакторе
code .github/workflows/security-scan.yml
# или
nano .github/workflows/security-scan.yml
```

### 5.2. Найдите шаг "Download Pre-populated Database"

Найдите этот блок в workflow:

```yaml
      - name: Download Pre-populated Database (Optional)
        continue-on-error: true
        run: |
          # Если у вас есть готовая база данных в GitHub Releases или другом источнике
          # Раскомментируйте и укажите URL:
          # mkdir -p ~/.dependency-check/data
          # wget -q YOUR_DATABASE_URL -O /tmp/db.tar.gz
          # tar -xzf /tmp/db.tar.gz -C ~/.dependency-check/data
          echo "ℹ️ Pre-populated database download skipped (not configured)"
```

### 5.3. Раскомментируйте и настройте

Замените на:

```yaml
      - name: Download Pre-populated Database
        continue-on-error: true
        run: |
          echo "📥 Downloading pre-populated database..."
          mkdir -p ~/.dependency-check/data
          
          # Скачиваем базу данных из GitHub Releases
          wget -q https://github.com/DevSecOps-F25/DevSecOps/releases/download/v1.0.0-db/dependency-check-db.tar.gz -O /tmp/db.tar.gz
          
          # Распаковываем
          tar -xzf /tmp/db.tar.gz -C ~/.dependency-check/data
          
          # Проверяем результат
          DB_FILES=$(find ~/.dependency-check/data -type f 2>/dev/null | wc -l)
          if [ "$DB_FILES" -gt 50 ]; then
            echo "✅ Database downloaded successfully ($DB_FILES files)"
          else
            echo "⚠️ Database download incomplete ($DB_FILES files)"
          fi
          
          # Удаляем временный файл
          rm -f /tmp/db.tar.gz
```

**Важно:** Замените URL на ваш реальный URL из GitHub Releases!

---

## Шаг 6: Тестирование

### 6.1. Закоммитьте изменения

```bash
cd /Users/MojPK/Downloads/University/NCS/DevSecOps
git add .github/workflows/security-scan.yml
git commit -m "Add pre-populated database download from GitHub Releases"
git push origin master
```

### 6.2. Запустите workflow

- Перейдите на GitHub: `https://github.com/DevSecOps-F25/DevSecOps/actions`
- Запустите workflow вручную или дождитесь автоматического запуска

### 6.3. Проверьте логи

В логах workflow вы должны увидеть:
```
📥 Downloading pre-populated database...
✅ Database downloaded successfully (XXX files)
```

---

## Шаг 7: Обновление базы данных (опционально)

Если нужно обновить базу данных:

1. **Локально обновите базу:**
   ```bash
   ~/dependency-check/dependency-check/bin/dependency-check.sh --updateonly
   ```

2. **Создайте новый архив:**
   ```bash
   cd ~/.dependency-check
   tar -czf dependency-check-db.tar.gz data/
   ```

3. **Создайте новый релиз:**
   - Используйте новый tag (например, `v1.0.1-db`)
   - Загрузите новый архив

4. **Обновите URL в workflow:**
   - Измените версию в URL на новую

---

## Альтернатива: Использование другого источника

Если GitHub Releases недоступен, можно использовать другой источник:

### Google Drive

1. Загрузите файл в Google Drive
2. Сделайте его публичным
3. Получите прямую ссылку для скачивания
4. Используйте в workflow:

```yaml
wget -q "https://drive.google.com/uc?export=download&id=YOUR_FILE_ID" -O /tmp/db.tar.gz
```

### Dropbox

1. Загрузите файл в Dropbox
2. Создайте публичную ссылку
3. Замените `www.dropbox.com` на `dl.dropboxusercontent.com` в URL
4. Используйте в workflow

---

## Проверка работоспособности

После настройки проверьте:

1. ✅ База данных скачивается в workflow
2. ✅ Количество файлов больше 50
3. ✅ Сканирование выполняется с `--noupdate`
4. ✅ Отчёты содержат данные об уязвимостях

---

## Troubleshooting

### Проблема: "404 Not Found" при скачивании

**Решение:** Проверьте URL релиза. Убедитесь, что:
- Релиз опубликован (не draft)
- Файл прикреплён к релизу
- URL правильный

### Проблема: База данных не распаковывается

**Решение:** Проверьте формат архива. Используйте:
```bash
file /tmp/db.tar.gz  # Должно быть "gzip compressed data"
```

### Проблема: База данных пустая после распаковки

**Решение:** Проверьте структуру архива:
```bash
tar -tzf /tmp/db.tar.gz | head -10
```

Должно быть: `data/...` (файлы внутри директории `data`)

---

## Итог

После выполнения всех шагов:
1. ✅ База данных обновлена локально
2. ✅ База данных загружена в GitHub Releases
3. ✅ Workflow настроен для скачивания базы
4. ✅ Сканирование работает с полной базой данных

Теперь workflow будет работать быстро и с полными данными об уязвимостях!

