name: Security Scan

on: [push, pull_request]

jobs:
  sonarqube:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v4
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  dependency-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache Dependency Check Database
        uses: actions/cache@v3
        with:
          path: ~/.dependency-check/data
          key: dependency-check-db-${{ runner.os }}-${{ hashFiles('**/composer.lock', '**/package-lock.json') }}
          restore-keys: |
            dependency-check-db-${{ runner.os }}-

      - name: Download and Install OWASP Dependency Check
        run: |
          echo "📦 Downloading OWASP Dependency Check..."
          # Скачиваем версию 10.0.0
          wget -q https://github.com/jeremylong/DependencyCheck/releases/download/v10.0.0/dependency-check-10.0.0-release.zip
          unzip -q dependency-check-10.0.0-release.zip -d dependency-check
          chmod +x dependency-check/dependency-check/bin/dependency-check.sh
          
          echo "✅ Dependency Check installed"
          # Проверяем версию и доступные аргументы
          ./dependency-check/dependency-check/bin/dependency-check.sh --version

      - name: Run Dependency Check Scan
        run: |
          echo "🔍 Starting Dependency Check scan..."
          
          # Создаем папку для отчетов
          mkdir -p reports
          
          # ИСПРАВЛЕННАЯ КОМАНДА: убраны несуществующие аргументы
          ./dependency-check/dependency-check/bin/dependency-check.sh \
            --project "DVWA Security Scan" \
            --scan ./app \
            --format HTML \
            --format JSON \
            --out ./reports \
            --disableNodeAudit \
            --disableNodeJS \
            --disableRetireJS \
            --disableOssIndex \
            --nvdApiDelay 6000 \          # Пауза между запросами к NVD (в миллисекундах)
            --nvdMaxRetryCount 10 \       # Количество попыток при ошибках сети
            --failOnCVSS 0                 # Завершить с ошибкой при любой найденной уязвимости
          
          echo "✅ Scan completed!"

      - name: Check Generated Reports
        run: |
          echo "📊 Checking generated reports..."
          if [ -f "reports/dependency-check-report.html" ]; then
            echo "✅ HTML report generated: $(ls -lh reports/dependency-check-report.html)"
            echo "--- Report Summary ---"
            # Извлекаем ключевую информацию из HTML-отчета
            grep -i "vulnerabilities\|dependencies" reports/dependency-check-report.html | head -5
          else
            echo "❌ HTML report not found"
          fi
          
          if [ -f "reports/dependency-check-report.json" ]; then
            echo "✅ JSON report generated: $(ls -lh reports/dependency-check-report.json)"
            echo "--- JSON Summary ---"
            # Быстрая проверка содержимого JSON
            python3 << 'EOF'
import json, os
try:
    if os.path.exists('reports/dependency-check-report.json'):
        with open('reports/dependency-check-report.json', 'r') as f:
            data = json.load(f)
        deps = len(data.get("dependencies", []))
        vulns = sum(len(d.get("vulnerabilities", [])) for d in data.get("dependencies", []))
        print(f"Scanned Dependencies: {deps}")
        print(f"Total Vulnerabilities Found: {vulns}")
        if vulns > 0:
            # Показываем примеры уязвимостей
            for dep in data.get("dependencies", []):
                if dep.get("vulnerabilities"):
                    print(f"\nFound in: {dep.get('fileName', 'Unknown')}")
                    for v in dep.get("vulnerabilities", [])[:2]:  # Первые 2 уязвимости
                        print(f"  - {v.get('name', 'Unknown')}: {v.get('description', 'No description')[:100]}...")
                    break
except Exception as e:
    print(f"Error reading JSON: {e}")
EOF
          else
            echo "❌ JSON report not found"
          fi

      - name: Upload Security Reports
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: |
            reports/
          retention-days: 30
          if-no-files-found: warn

  security-checks:
    runs-on: ubuntu-latest
    needs: [sonarqube, dependency-check]
    steps:
      - name: Security Scan Summary
        run: |
          echo "========================================"
          echo "✅ All security scans completed successfully!"
          echo "📁 Available reports:"
          echo "   - SonarQube (SAST) results: Check SonarCloud dashboard"
          echo "   - Dependency Check (SCA) reports: Download 'security-reports' artifact"
          echo "========================================"